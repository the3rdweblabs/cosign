{
  "name": "cosign-facilitator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cosign x402 facilitator + native EOA paymaster for BOT Chain",
  "scripts": {
    "start": "tsx --env-file-if-exists=.env src/server.ts",
    "dev": "tsx watch --env-file-if-exists=.env src/server.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@xbot02/core": "file:./sdk/packages/core",
    "viem": "^2.55.11"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.10",
    "typescript": "^5.9.0"
  }
}